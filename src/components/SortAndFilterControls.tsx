import { Typography } from '@mui/material'
import React from 'react'

const SortAndFilterControls: React.FC = (): JSX.Element => {
  return (
    <Typography variant="h6" sx={{ py: 4 }}>
      Sort And Filter Controls
    </Typography>
  )
}

export default SortAndFilterControls
